{
  "name": "sst-triage-ts-jest",
  "version": "0.1.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "sst test",
    "start": "sst start",
    "build": "sst build",
    "deploy": "sst deploy",
    "remove": "sst remove"
  },
  "devDependencies": {
    "@aws-cdk/assert": "1.114.0",
    "lerna": "^3.22.1"
  },
  "dependencies": {
    "@serverless-stack/cli": "0.40.5",
    "@serverless-stack/resources": "0.40.5",
    "@aws-cdk/core": "1.114.0"
  }
}
